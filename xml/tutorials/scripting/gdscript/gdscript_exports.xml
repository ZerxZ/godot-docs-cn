<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.21.2 -->
<document source="/home/runner/work/godot-docs-cn/godot-docs-cn/godot-docs/docs/tutorials/scripting/gdscript/gdscript_exports.rst" translation_progress="{'total': 0, 'translated': 0}" xmlns:c="https://www.sphinx-doc.org/" xmlns:changeset="https://www.sphinx-doc.org/" xmlns:citation="https://www.sphinx-doc.org/" xmlns:cpp="https://www.sphinx-doc.org/" xmlns:index="https://www.sphinx-doc.org/" xmlns:js="https://www.sphinx-doc.org/" xmlns:math="https://www.sphinx-doc.org/" xmlns:py="https://www.sphinx-doc.org/" xmlns:rst="https://www.sphinx-doc.org/" xmlns:std="https://www.sphinx-doc.org/">
    <target refid="doc-gdscript-exports"></target>
    <section ids="gdscript-exported-properties doc-gdscript-exports" names="gdscript\ exported\ properties doc_gdscript_exports">
        <title>GDScript exported properties</title>
        <paragraph>In Godot, class members can be exported. This means their value gets saved along
            with the resource (such as the <reference internal="True" refuri="../../../classes/class_packedscene#class-packedscene"><inline classes="std std-ref">scene</inline></reference>) they're
            attached to, and get transferred over when using <reference internal="True" refuri="../../networking/high_level_multiplayer#doc-high-level-multiplayer-rpcs"><inline classes="std std-ref">RPCs</inline></reference>.
            They will also be available for editing in the property editor. Exporting is done by using
            the <literal>@export</literal> annotation.</paragraph>
        <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var number: int = 5</literal_block>
        <paragraph>In that example the value <literal>5</literal> will be saved and visible in the property editor.</paragraph>
        <paragraph>An exported variable must be initialized to a constant expression or have a type specifier
            in the variable. Some of the export annotations have a specific type and don't need the variable to be typed (see the
            <emphasis>Examples</emphasis> section below).</paragraph>
        <paragraph>One of the fundamental benefits of exporting member variables is to have
            them visible and editable in the editor. This way, artists and game designers
            can modify values that later influence how the program runs. For this, a
            special export syntax is provided.</paragraph>
        <note>
            <paragraph>Exporting properties can also be done in other languages such as C#.
                The syntax varies depending on the language. See <reference internal="True" refuri="../c_sharp/c_sharp_exports#doc-c-sharp-exports"><inline classes="std std-ref">C# exported properties</inline></reference>
                for information on C# exports.</paragraph>
        </note>
        <section ids="basic-use" names="basic\ use">
            <title>Basic use</title>
            <paragraph>If the exported value assigns a constant or constant expression,
                the type will be inferred and used in the editor.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var number = 5</literal_block>
            <paragraph>If there's no default value, you can add a type to the variable.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var number: int</literal_block>
            <paragraph>Resources and nodes can be exported.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var resource: Resource
@export var node: Node</literal_block>
            <paragraph>Even if a script is not executed in the editor, exported properties
                can still be edited. However, getters and setters will only be used
                if the script is in <reference internal="True" refuri="gdscript_basics#doc-gdscript-tool-mode"><inline classes="std std-ref">Tool mode</inline></reference>.</paragraph>
        </section>
        <section ids="grouping-exports" names="grouping\ exports">
            <title>Grouping exports</title>
            <paragraph>It is possible to group your exported properties inside the Inspector
                with the <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-group"><inline classes="std std-ref">@export_group</inline></reference>
                annotation. Every exported property after this annotation will be added to
                the group. Start a new group or use <literal>@export_group("")</literal> to break out.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_group("My Properties")
@export var number = 3</literal_block>
            <paragraph>The second argument of the annotation can be used to only group properties
                with the specified prefix.</paragraph>
            <paragraph>Groups cannot be nested, use <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-subgroup"><inline classes="std std-ref">@export_subgroup</inline></reference>
                to create subgroups within a group.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_subgroup("Extra Properties")
@export var string = ""
@export var flag = false</literal_block>
            <paragraph>You can also change the name of your main category, or create additional
                categories in the property list with the <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-category"><inline classes="std std-ref">@export_category</inline></reference>
                annotation.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_category("Main Category")
@export var number = 3
@export var string = ""

@export_category("Extra Category")
@export var flag = false</literal_block>
            <note>
                <paragraph>The list of properties is organized based on the class inheritance and
                    new categories break that expectation. Use them carefully, especially
                    when creating projects for public use.</paragraph>
            </note>
        </section>
        <section ids="strings-as-paths" names="strings\ as\ paths">
            <title>Strings as paths</title>
            <paragraph>String as a path to a file. See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-file"><inline classes="std std-ref">@export_file</inline></reference>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_file var f</literal_block>
            <paragraph>String as a path to a directory. See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-dir"><inline classes="std std-ref">@export_dir</inline></reference>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_dir var f</literal_block>
            <paragraph>String as a path to a file, custom filter provided as hint. See again <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-file"><inline classes="std std-ref">@export_file</inline></reference>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_file("*.txt") var f</literal_block>
            <paragraph>Using paths in the global filesystem is also possible,
                but only in scripts in tool mode.</paragraph>
            <paragraph>String as a path to a PNG file in the global filesystem. See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-global-file"><inline classes="std std-ref">@export_global_file</inline></reference>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_global_file("*.png") var tool_image</literal_block>
            <paragraph>String as a path to a directory in the global filesystem. See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-global-dir"><inline classes="std std-ref">@export_global_dir</inline></reference>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_global_dir var tool_dir</literal_block>
            <paragraph>The multiline annotation tells the editor to show a large input
                field for editing over multiple lines. See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-multiline"><inline classes="std std-ref">@export_multiline</inline></reference>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_multiline var text</literal_block>
        </section>
        <section ids="limiting-editor-input-ranges" names="limiting\ editor\ input\ ranges">
            <title>Limiting editor input ranges</title>
            <paragraph>See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-range"><inline classes="std std-ref">@export_range</inline></reference> for all of the following.</paragraph>
            <paragraph>Allow integer values from 0 to 20.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(0, 20) var i</literal_block>
            <paragraph>Allow integer values from -10 to 20.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(-10, 20) var j</literal_block>
            <paragraph>Allow floats from -10 to 20 and snap the value to multiples of 0.2.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(-10, 20, 0.2) var k: float</literal_block>
            <paragraph>The limits can be made to affect only the slider if you add the hints <literal>"or_less"</literal>
                and/or <literal>"or_greater"</literal>. If either these hints are used, it will be possible for
                the user to enter any value or drag the value with the mouse when not using
                the slider, even if outside the specified range.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(0, 100, 1, "or_less", "or_greater") var l: int</literal_block>
            <paragraph>The <literal>"exp"</literal> hint can be used to make a value have an exponential slider
                instead of a linear slider. This means that when dragging the slider towards
                the right, changes will become progressively faster when dragging the mouse.
                This is useful to make editing values that can be either very small or very large
                easier, at the cost of being less intuitive.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(0, 100000, 0.01, "exp") var exponential: float</literal_block>
            <paragraph>For values that are meant to represent an easing factor, use
                <reference internal="True" refid="doc-gdscript-exports-floats-with-easing-hint"><inline classes="std std-ref">Floats with easing hint</inline></reference> instead.</paragraph>
            <paragraph>The <literal>"hide_slider"</literal> hint can be used to hide the horizontal bar that
                appears below <literal>float</literal> properties, or the up/down arrows that appear besides
                <literal>int</literal> properties:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(0, 1000, 0.01, "hide_slider") var no_slider: float</literal_block>
        </section>
        <section ids="adding-suffixes-and-handling-degrees-radians" names="adding\ suffixes\ and\ handling\ degrees/radians">
            <title>Adding suffixes and handling degrees/radians</title>
            <paragraph>A suffix can also be defined to make the value more self-explanatory in the
                inspector. For example, to define a value that is meant to be configured as
                "meters" (<literal>m</literal>) by the user:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(0, 100, 1, "suffix:m") var m: int</literal_block>
            <paragraph>For angles that are stored in radians but displayed as degrees to the user, use
                the <title_reference>"radians_as_degrees"</title_reference> hint:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(0, 360, 0.1, "radians_as_degrees") var angle: float</literal_block>
            <paragraph>This performs automatic conversion when the value is displayed or modified in
                the inspector and also displays a degree (<literal>°</literal>) suffix. This approach is used
                by Godot's own <title_reference>rotation</title_reference> properties throughout the editor.</paragraph>
            <paragraph>If the angle is stored in degrees instead, use the <title_reference>"degrees"</title_reference> hint to display
                the degree symbol while disabling the automatic degrees-to-radians conversion
                when the value is modified from the inspector.</paragraph>
            <target refid="doc-gdscript-exports-floats-with-easing-hint"></target>
        </section>
        <section ids="floats-with-easing-hint doc-gdscript-exports-floats-with-easing-hint" names="floats\ with\ easing\ hint doc_gdscript_exports_floats_with_easing_hint">
            <title>Floats with easing hint</title>
            <paragraph>Display a visual representation of the <literal>ease()</literal> function
                when editing. See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-exp-easing"><inline classes="std std-ref">@export_exp_easing</inline></reference>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_exp_easing var transition_speed</literal_block>
        </section>
        <section ids="colors" names="colors">
            <title>Colors</title>
            <paragraph>Regular color given as red-green-blue-alpha value.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var col: Color</literal_block>
            <paragraph>Color given as red-green-blue value (alpha will always be 1). See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-color-no-alpha"><inline classes="std std-ref">@export_color_no_alpha</inline></reference>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_color_no_alpha var col: Color</literal_block>
        </section>
        <section ids="nodes" names="nodes">
            <title>Nodes</title>
            <paragraph>Nodes can also be directly exported as properties in a script without
                having to use NodePaths:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve"># Allows any node.
@export var node: Node

# Allows any node that inherits from BaseButton.
# Custom classes declared with `class_name` can also be used.
@export var some_button: BaseButton</literal_block>
            <paragraph>Exporting NodePaths like in Godot 3.x is still possible, in case you need it:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var node_path: NodePath
var node = get_node(node_path)</literal_block>
            <paragraph>If you want to limit the types of nodes for NodePaths, you can use the
                <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-node-path"><inline classes="std std-ref">@export_node_path</inline></reference>
                annotation:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_node_path("Button", "TouchScreenButton") var some_button</literal_block>
        </section>
        <section ids="resources" names="resources">
            <title>Resources</title>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var resource: Resource</literal_block>
            <paragraph>In the Inspector, you can then drag and drop a resource file
                from the FileSystem dock into the variable slot.</paragraph>
            <paragraph>Opening the inspector dropdown may result in an
                extremely long list of possible classes to create, however.
                Therefore, if you specify an extension of Resource such as:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var resource: AnimationNode</literal_block>
            <paragraph>The drop-down menu will be limited to AnimationNode and all
                its derived classes.</paragraph>
            <target refid="doc-gdscript-exports-exporting-bit-flags"></target>
        </section>
        <section ids="exporting-bit-flags doc-gdscript-exports-exporting-bit-flags" names="exporting\ bit\ flags doc_gdscript_exports_exporting_bit_flags">
            <title>Exporting bit flags</title>
            <paragraph>See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-flags"><inline classes="std std-ref">@export_flags</inline></reference>.</paragraph>
            <paragraph>Integers used as bit flags can store multiple <literal>true</literal>/<literal>false</literal> (boolean)
                values in one property. By using the <literal>@export_flags</literal> annotation, they
                can be set from the editor:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve"># Set any of the given flags from the editor.
@export_flags("Fire", "Water", "Earth", "Wind") var spell_elements = 0</literal_block>
            <paragraph>You must provide a string description for each flag. In this example, <literal>Fire</literal>
                has value 1, <literal>Water</literal> has value 2, <literal>Earth</literal> has value 4 and <literal>Wind</literal>
                corresponds to value 8. Usually, constants should be defined accordingly (e.g.
                <literal>const ELEMENT_WIND = 8</literal> and so on).</paragraph>
            <paragraph>You can add explicit values using a colon:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_flags("Self:4", "Allies:8", "Foes:16") var spell_targets = 0</literal_block>
            <paragraph>Only power of 2 values are valid as bit flags options. The lowest allowed value
                is 1, as 0 means that nothing is selected. You can also add options that are a
                combination of other flags:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_flags("Self:4", "Allies:8", "Self and Allies:12", "Foes:16")
var spell_targets = 0</literal_block>
            <paragraph>Export annotations are also provided for the physics, render, and navigation layers defined in the project settings:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_flags_2d_physics var layers_2d_physics
@export_flags_2d_render var layers_2d_render
@export_flags_2d_navigation var layers_2d_navigation
@export_flags_3d_physics var layers_3d_physics
@export_flags_3d_render var layers_3d_render
@export_flags_3d_navigation var layers_3d_navigation</literal_block>
            <paragraph>Using bit flags requires some understanding of bitwise operations.
                If in doubt, use boolean variables instead.</paragraph>
        </section>
        <section ids="exporting-enums" names="exporting\ enums">
            <title>Exporting enums</title>
            <paragraph>See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-enum"><inline classes="std std-ref">@export_enum</inline></reference>.</paragraph>
            <paragraph>Properties can be exported with a type hint referencing an enum to limit their values
                to the values of the enumeration. The editor will create a widget in the Inspector, enumerating
                the following as "Thing 1", "Thing 2", "Another Thing". The value will be stored as an integer.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">enum NamedEnum {THING_1, THING_2, ANOTHER_THING = -1}
@export var x: NamedEnum</literal_block>
            <paragraph>Integer and string properties can also be limited to a specific list of values using
                the <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-enum"><inline classes="std std-ref">@export_enum</inline></reference> annotation.
                The editor will create a widget in the Inspector, enumerating the following as Warrior,
                Magician, Thief. The value will be stored as an integer, corresponding to the index
                of the selected option (i.e. <literal>0</literal>, <literal>1</literal>,  or <literal>2</literal>).</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_enum("Warrior", "Magician", "Thief") var character_class: int</literal_block>
            <paragraph>You can add explicit values using a colon:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_enum("Slow:30", "Average:60", "Very Fast:200") var character_speed: int</literal_block>
            <paragraph>If the type is String, the value will be stored as a string.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_enum("Rebecca", "Mary", "Leah") var character_name: String</literal_block>
            <paragraph>If you want to set an initial value, you must specify it explicitly:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_enum("Rebecca", "Mary", "Leah") var character_name: String = "Rebecca"</literal_block>
        </section>
        <section ids="exporting-arrays" names="exporting\ arrays">
            <title>Exporting arrays</title>
            <paragraph>Exported arrays can have initializers, but they must be constant expressions.</paragraph>
            <paragraph>If the exported array specifies a type which inherits from Resource, the array
                values can be set in the inspector by dragging and dropping multiple files
                from the FileSystem dock at once.</paragraph>
            <paragraph>The default value <strong>must</strong> be a constant expression.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var a = [1, 2, 3]</literal_block>
            <comment xml:space="preserve">UPDATE: Not supported yet. When nested typed arrays are supported, update</comment>
            <comment xml:space="preserve">the example.</comment>
            <paragraph>Exported arrays can specify type (using the same hints as before).</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var ints: Array[int] = [1, 2, 3]

# Nested typed arrays such as `Array[Array[float]]` are not supported yet.
@export var two_dimensional: Array[Array] = [[1.0, 2.0], [3.0, 4.0]]</literal_block>
            <paragraph>You can omit the default value, but it would then be <literal>null</literal> if not assigned.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var b: Array
@export var scenes: Array[PackedScene]</literal_block>
            <paragraph>Arrays with specified types which inherit from resource can be set by
                drag-and-dropping multiple files from the FileSystem dock.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var textures: Array[Texture] = []
@export var scenes: Array[PackedScene] = []</literal_block>
            <paragraph>Packed type arrays also work, but only initialized empty:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export var vector3s = PackedVector3Array()
@export var strings = PackedStringArray()</literal_block>
            <paragraph>Other export variants can also be used when exporting arrays:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_range(-360, 360, 0.001, "degrees") var laser_angles: Array[float] = []
@export_file("*.json") var skill_trees: Array[String] = []
@export_color_no_alpha var hair_colors = PackedColorArray()
@export_enum("Espresso", "Mocha", "Latte", "Capuccino") var barista_suggestions: Array[String] = []</literal_block>
        </section>
        <section ids="export-storage" names="@export_storage">
            <title><literal>@export_storage</literal></title>
            <paragraph>See <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-storage"><inline classes="std std-ref">@export_storage</inline></reference>.</paragraph>
            <paragraph>By default, exporting a property has two effects:</paragraph>
            <enumerated_list enumtype="arabic" prefix="" suffix=".">
                <list_item>
                    <paragraph>makes the property stored in the scene/resource file (<reference internal="True" refuri="../../../classes/class_@globalscope#class-globalscope-constant-property-usage-storage"><inline classes="std std-ref">PROPERTY_USAGE_STORAGE</inline></reference>);</paragraph>
                </list_item>
                <list_item>
                    <paragraph>adds a field to the Inspector (<reference internal="True" refuri="../../../classes/class_@globalscope#class-globalscope-constant-property-usage-editor"><inline classes="std std-ref">PROPERTY_USAGE_EDITOR</inline></reference>).</paragraph>
                </list_item>
            </enumerated_list>
            <paragraph>However, sometimes you may want to make a property serializable, but not display it
                in the editor to prevent unintentional changes and cluttering the interface.</paragraph>
            <paragraph>To do this you can use <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-storage"><inline classes="std std-ref">@export_storage</inline></reference>.
                This can be useful for <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-tool"><inline classes="std std-ref">@tool</inline></reference> scripts.
                Also the property value is copied when <reference internal="True" refuri="../../../classes/class_resource#class-resource-method-duplicate"><inline classes="std std-ref">Resource.duplicate()</inline></reference>
                or <reference internal="True" refuri="../../../classes/class_node#class-node-method-duplicate"><inline classes="std std-ref">Node.duplicate()</inline></reference> is called, unlike non-exported variables.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">var a # Not stored in the file, not displayed in the editor.
@export_storage var b # Stored in the file, not displayed in the editor.
@export var c: int # Stored in the file, displayed in the editor.</literal_block>
        </section>
        <section ids="export-custom" names="@export_custom">
            <title><literal>@export_custom</literal></title>
            <paragraph>If you need more control than what's exposed with the built-in <literal>@export</literal>
                annotations, you can use <literal>@export_custom</literal> instead. This allows defining any
                property hint, hint string and usage flags, with a syntax similar to the one
                used by the editor for built-in nodes.</paragraph>
            <paragraph>For example, this exposes the <literal>altitude</literal> property with no range limits but an
                <literal>m</literal> (meter) suffix defined:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@export_custom(PROPERTY_HINT_NONE, "suffix:m") var altitude: float</literal_block>
            <paragraph>The above is normally not feasible with the standard <literal>@export_range</literal> syntax,
                since it requires defining a range.</paragraph>
            <paragraph>See the <reference internal="True" refuri="../../../classes/class_@gdscript#class-gdscript-annotation-export-custom"><inline classes="std std-ref">class reference</inline></reference>
                for a list of parameters and their allowed values.</paragraph>
            <warning>
                <paragraph>When using <literal>@export_custom</literal>, GDScript does not perform any validation on
                    the syntax. Invalid syntax may have unexpected behavior in the inspector.</paragraph>
            </warning>
        </section>
        <section ids="export-tool-button" names="@export_tool_button">
            <title><literal>@export_tool_button</literal></title>
            <paragraph>If you need to create a clickable inspector button, you can use <literal>@export_tool_button</literal>.
                This exports a <literal>Callable</literal> property as a clickable button. When the button is pressed, the callable is called.</paragraph>
            <paragraph>You can specify a custom icon name, which must match one of the icon
                file names from the
                <reference name="editor/icons" refuri="https://github.com/godotengine/godot/tree/master/editor/icons">editor/icons</reference>
                folder of the Godot source repository (case-sensitive).
                You can also browse the editor icons using the
                <reference name="Godot editor icons" refuri="https://godot-editor-icons.github.io/">Godot editor icons</reference> website.</paragraph>
            <paragraph>For example, if you wish to use <literal>Node2D.svg</literal> from that folder, you must
                specify <literal>"Node2D"</literal> as the second parameter of <literal>@export_tool_button</literal>. It is
                not currently possible to use custom icons from the project folder; only
                built-in editor icons can be used.</paragraph>
            <paragraph>This exports a button with label <literal>"Hello"</literal> and icon <literal>"Callable"</literal> (which is the
                default if no icon is specified). When you press it, it will print <literal>"Hello
world!"</literal>.</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve">@tool
extends Node

@export_tool_button("Hello", "Callable") var hello_action = hello

func hello():
    print("Hello world!")</literal_block>
        </section>
        <section ids="setting-exported-variables-from-a-tool-script" names="setting\ exported\ variables\ from\ a\ tool\ script">
            <title>Setting exported variables from a tool script</title>
            <paragraph>When changing an exported variable's value from a script in
                <reference internal="True" refuri="gdscript_basics#doc-gdscript-tool-mode"><inline classes="std std-ref">Tool mode</inline></reference>, the value in the inspector won't be updated
                automatically. To update it, call
                <reference internal="True" refuri="../../../classes/class_object#class-object-method-notify-property-list-changed"><inline classes="std std-ref">notify_property_list_changed()</inline></reference>
                after setting the exported variable's value.</paragraph>
        </section>
        <section ids="reading-an-exported-variable-s-value-early-on" names="reading\ an\ exported\ variable's\ value\ early\ on">
            <title>Reading an exported variable's value early on</title>
            <paragraph>If you read an exported variable's value in <reference internal="True" refuri="../../../classes/class_object#class-object-private-method-init"><inline classes="std std-ref">_init()</inline></reference>,
                it will return the default value specified in the export annotation instead of the value
                that was set in the inspector. This is because assigning values from the saved scene/resource
                file occurs <emphasis>after</emphasis> object initialization; until then, the default value is used.</paragraph>
            <paragraph>To get the value that was set in the inspector (and therefore saved in the scene/resource file),
                you need to read it <emphasis>after</emphasis> the object is constructed, such as in
                <reference internal="True" refuri="../../../classes/class_node#class-node-private-method-ready"><inline classes="std std-ref">Node._ready()</inline></reference>. You can also read the value
                in a setter that's defined on the exported property, which is useful in
                custom resources where <literal>_ready()</literal> is not available:</paragraph>
            <literal_block force="False" language="gdscript" linenos="False" xml:space="preserve"># Set this property to 3 in the inspector.
@export var exported_variable = 2:
    set(value):
        exported_variable = value
        print("Inspector-set value: ", exported_variable)

func _init():
    print("Initial value: ", exported_variable)</literal_block>
            <paragraph>Results in:</paragraph>
            <literal_block force="False" highlight_args="{}" language="none" linenos="False" xml:space="preserve">Initial value: 2
Inspector-set value: 3</literal_block>
        </section>
        <section ids="advanced-exports" names="advanced\ exports">
            <title>Advanced exports</title>
            <paragraph>Not every type of export can be provided on the level of the language itself to
                avoid unnecessary design complexity. The following describes some more or less
                common exporting features which can be implemented with a low-level API.</paragraph>
            <paragraph>Before reading further, you should get familiar with the way properties are
                handled and how they can be customized with
                <reference internal="True" refuri="../../../classes/class_object#class-object-private-method-set"><inline classes="std std-ref">_set()</inline></reference>,
                <reference internal="True" refuri="../../../classes/class_object#class-object-private-method-get"><inline classes="std std-ref">_get()</inline></reference>, and
                <reference internal="True" refuri="../../../classes/class_object#class-object-private-method-get-property-list"><inline classes="std std-ref">_get_property_list()</inline></reference> methods as
                described in <reference internal="True" refuri="../../best_practices/godot_interfaces#doc-accessing-data-or-logic-from-object"><inline classes="std std-ref">Accessing data or logic from an object</inline></reference>.</paragraph>
            <seealso>
                <paragraph>For binding properties using the above methods in C++, see
                    <reference internal="True" refuri="../../../engine_details/architecture/object_class#doc-binding-properties-using-set-get-property-list"><inline classes="std std-ref">Binding properties using _set/_get/_get_property_list</inline></reference>.</paragraph>
            </seealso>
            <warning>
                <paragraph>The script must operate in the <literal>@tool</literal> mode so the above methods
                    can work from within the editor.</paragraph>
            </warning>
            <substitution_definition names="weblate_widget"><reference refuri="https://hosted.weblate.org/engage/godot-engine/?utm_source=widget"><image alt="Translation status" candidates="{'?': 'https://hosted.weblate.org/widgets/godot-engine/-/godot-docs/287x66-white.png'}" height="66" uri="https://hosted.weblate.org/widgets/godot-engine/-/godot-docs/287x66-white.png" width="287"></image></reference></substitution_definition>
        </section>
    </section>
</document>
