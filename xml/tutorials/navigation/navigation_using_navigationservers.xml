<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.21.2 -->
<document source="/home/runner/work/godot-docs-cn/godot-docs-cn/godot-docs/docs/tutorials/navigation/navigation_using_navigationservers.rst" translation_progress="{'total': 0, 'translated': 0}" xmlns:c="https://www.sphinx-doc.org/" xmlns:changeset="https://www.sphinx-doc.org/" xmlns:citation="https://www.sphinx-doc.org/" xmlns:cpp="https://www.sphinx-doc.org/" xmlns:index="https://www.sphinx-doc.org/" xmlns:js="https://www.sphinx-doc.org/" xmlns:math="https://www.sphinx-doc.org/" xmlns:py="https://www.sphinx-doc.org/" xmlns:rst="https://www.sphinx-doc.org/" xmlns:std="https://www.sphinx-doc.org/">
    <target refid="doc-navigation-using-navigationservers"></target>
    <section ids="using-navigationserver doc-navigation-using-navigationservers" names="using\ navigationserver doc_navigation_using_navigationservers">
        <title>Using NavigationServer</title>
        <paragraph>2D and 3D version of the NavigationServer are available as
            <reference internal="True" refuri="../../classes/class_navigationserver2d#class-navigationserver2d"><inline classes="std std-ref">NavigationServer2D</inline></reference> and
            <reference internal="True" refuri="../../classes/class_navigationserver3d#class-navigationserver3d"><inline classes="std std-ref">NavigationServer3D</inline></reference> respectively.</paragraph>
        <section ids="communicating-with-the-navigationserver" names="communicating\ with\ the\ navigationserver">
            <title>Communicating with the NavigationServer</title>
            <paragraph>To work with the NavigationServer means to prepare parameters for a <strong>query</strong> that can be sent to the NavigationServer for updates or requesting data.</paragraph>
            <paragraph>To reference the internal NavigationServer objects like maps, regions and agents RIDs are used as identification numbers.
                Every navigation related node in the scene tree has a function that returns the RID for this node.</paragraph>
        </section>
        <section ids="threading-and-synchronization" names="threading\ and\ synchronization">
            <title>Threading and Synchronization</title>
            <paragraph>The NavigationServer does not update every change immediately but waits until
                the end of the <strong>physics frame</strong> to synchronize all the changes together.</paragraph>
            <paragraph>Waiting for synchronization is required to apply changes to all maps, regions and agents.
                Synchronization is done because some updates like a recalculation of the entire navigation map are very expensive and require updated data from all other objects.
                Also the NavigationServer uses a <strong>threadpool</strong> by default for some functionality like avoidance calculation between agents.</paragraph>
            <paragraph>Waiting is not required for most <literal>get()</literal> functions that only request data from the NavigationServer without making changes.
                Note that not all data will account for changes made in the same frame.
                E.g. if an avoidance agent changed the navigation map this frame the <literal>agent_get_map()</literal> function will still return the old map before the synchronization.
                The exception to this are nodes that store their values internally before sending the update to the NavigationServer.
                When a getter on a node is used for a value that was updated in the same frame it will return the already updated value stored on the node.</paragraph>
            <paragraph>The NavigationServer is <strong>thread-safe</strong> as it places all API calls that want to make changes in a queue to be executed in the synchronization phase.
                Synchronization for the NavigationServer happens in the middle of the physics frame after scene input from scripts and nodes are all done.</paragraph>
            <note>
                <paragraph>The important takeaway is that most NavigationServer changes take effect after the next physics frame and not immediately.
                    This includes all changes made by navigation related nodes in the scene tree or through scripts.</paragraph>
            </note>
            <note>
                <paragraph>All setters and delete functions require synchronization.</paragraph>
            </note>
        </section>
        <section ids="d-and-3d-navigationserver-differences" names="2d\ and\ 3d\ navigationserver\ differences">
            <title>2D and 3D NavigationServer differences</title>
            <paragraph>NavigationServer2D and NavigationServer3D are equivalent in functionality for their dimension.</paragraph>
            <paragraph>Technically it is possible to use the tools for creating navigation meshes in one dimension for the other
                dimension, e.g. baking a 2D navigation mesh with the 3D NavigationMesh when using
                flat 3D source geometry or creating 3D flat navigation meshes with the
                polygon outline draw tools of NavigationRegion2D and NavigationPolygons.</paragraph>
        </section>
        <section ids="waiting-for-synchronization" names="waiting\ for\ synchronization">
            <title>Waiting for synchronization</title>
            <paragraph>At the start of the game, a new scene or procedural navigation changes any path query to a NavigationServer will return empty or wrong.</paragraph>
            <paragraph>The navigation map is still empty or not updated at this point.
                All nodes from the scene tree need to first upload their navigation related data to the NavigationServer.
                Each added or changed map, region or agent need to be registered with the NavigationServer.
                Afterward the NavigationServer requires a <strong>physics frame</strong> for synchronization to update the maps, regions and agents.</paragraph>
            <paragraph>One workaround is to make a deferred call to a custom setup function (so all nodes are ready).
                The setup function makes all the navigation changes, e.g. adding procedural stuff.
                Afterwards the function waits for the next physics frame before continuing with path queries.</paragraph>
            <container classes="sphinx-tabs" type="tab-element">
                <div aria-label="Tabbed content" role="tablist">
                    <button aria-controls="panel-0-R0RTY3JpcHQ=" aria-selected="true" classes="sphinx-tabs-tab code-tab group-tab" ids="tab-0-R0RTY3JpcHQ=" name="R0RTY3JpcHQ=" role="tab" tabindex="0">GDScript</button>
                    <button aria-controls="panel-0-QyM=" aria-selected="false" classes="sphinx-tabs-tab code-tab group-tab" ids="tab-0-QyM=" name="QyM=" role="tab" tabindex="-1">C#</button>
                </div>
                <div aria-labelledby="tab-0-R0RTY3JpcHQ=" classes="sphinx-tabs-panel code-tab group-tab" ids="panel-0-R0RTY3JpcHQ=" name="R0RTY3JpcHQ=" role="tabpanel" tabindex="0">
                    <literal_block force="False" highlight_args="{}" language="gdscript" linenos="False" xml:space="preserve">extends Node3D

func _ready():
    # Use call deferred to make sure the entire scene tree nodes are setup
    # else await on 'physics_frame' in a _ready() might get stuck.
    custom_setup.call_deferred()

func custom_setup():

    # Create a new navigation map.
    var map: RID = NavigationServer3D.map_create()
    NavigationServer3D.map_set_up(map, Vector3.UP)
    NavigationServer3D.map_set_active(map, true)

    # Create a new navigation region and add it to the map.
    var region: RID = NavigationServer3D.region_create()
    NavigationServer3D.region_set_transform(region, Transform3D())
    NavigationServer3D.region_set_map(region, map)

    # Create a procedural navigation mesh for the region.
    var new_navigation_mesh: NavigationMesh = NavigationMesh.new()
    var vertices: PackedVector3Array = PackedVector3Array([
        Vector3(0, 0, 0),
        Vector3(9.0, 0, 0),
        Vector3(0, 0, 9.0)
    ])
    new_navigation_mesh.set_vertices(vertices)
    var polygon: PackedInt32Array = PackedInt32Array([0, 1, 2])
    new_navigation_mesh.add_polygon(polygon)
    NavigationServer3D.region_set_navigation_mesh(region, new_navigation_mesh)

    # Wait for NavigationServer sync to adapt to made changes.
    await get_tree().physics_frame

    # Query the path from the navigation server.
    var start_position: Vector3 = Vector3(0.1, 0.0, 0.1)
    var target_position: Vector3 = Vector3(1.0, 0.0, 1.0)
    var optimize_path: bool = true

    var path: PackedVector3Array = NavigationServer3D.map_get_path(
        map,
        start_position,
        target_position,
        optimize_path
    )

    print("Found a path!")
    print(path)</literal_block>
                </div>
                <div aria-labelledby="tab-0-QyM=" classes="sphinx-tabs-panel code-tab group-tab" hidden="true" ids="panel-0-QyM=" name="QyM=" role="tabpanel" tabindex="0">
                    <literal_block force="False" highlight_args="{}" language="csharp" linenos="False" xml:space="preserve">using Godot;

public partial class MyNode3D : Node3D
{
    public override void _Ready()
    {
        // Use call deferred to make sure the entire scene tree nodes are setup
        // else await on 'physics_frame' in a _Ready() might get stuck.
        CallDeferred(MethodName.CustomSetup);
    }

    private async void CustomSetup()
    {
        // Create a new navigation map.
        Rid map = NavigationServer3D.MapCreate();
        NavigationServer3D.MapSetUp(map, Vector3.Up);
        NavigationServer3D.MapSetActive(map, true);

        // Create a new navigation region and add it to the map.
        Rid region = NavigationServer3D.RegionCreate();
        NavigationServer3D.RegionSetTransform(region, Transform3D.Identity);
        NavigationServer3D.RegionSetMap(region, map);

        // Create a procedural navigation mesh for the region.
        var newNavigationMesh = new NavigationMesh()
        {
            Vertices =
            [
                new Vector3(0.0f, 0.0f, 0.0f),
                new Vector3(9.0f, 0.0f, 0.0f),
                new Vector3(0.0f, 0.0f, 9.0f),
            ],
        };
        int[] polygon = [0, 1, 2];
        newNavigationMesh.AddPolygon(polygon);
        NavigationServer3D.RegionSetNavigationMesh(region, newNavigationMesh);

        // Wait for NavigationServer sync to adapt to made changes.
        await ToSignal(GetTree(), SceneTree.SignalName.PhysicsFrame);

        // Query the path from the navigation server.
        var startPosition = new Vector3(0.1f, 0.0f, 0.1f);
        var targetPosition = new Vector3(1.0f, 0.0f, 1.0f);

        Vector3[] path = NavigationServer3D.MapGetPath(map, startPosition, targetPosition, optimize: true);

        GD.Print("Found a path!");
        GD.Print((Variant)path);
    }
}</literal_block>
                </div>
            </container>
        </section>
        <section ids="server-avoidance-callbacks" names="server\ avoidance\ callbacks">
            <title>Server Avoidance Callbacks</title>
            <paragraph>If RVO avoidance agents are registered for avoidance callbacks the NavigationServer dispatches
                their <literal>velocity_computed</literal> signals just before the PhysicsServer synchronization.</paragraph>
            <paragraph>To learn more about NavigationAgents see <reference internal="True" refuri="navigation_using_navigationagents#doc-navigation-using-navigationagents"><inline classes="std std-ref">Using NavigationAgents</inline></reference>.</paragraph>
            <paragraph>The simplified order of execution for NavigationAgents that use avoidance:</paragraph>
            <bullet_list bullet="-">
                <list_item>
                    <paragraph>physics frame starts.</paragraph>
                </list_item>
                <list_item>
                    <paragraph><literal>_physics_process(delta)</literal>.</paragraph>
                </list_item>
                <list_item>
                    <paragraph><literal>velocity</literal> property is set on NavigationAgent Node.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Agent sends velocity and position to NavigationServer.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>NavigationServer waits for synchronization.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>NavigationServer synchronizes and computes avoidance velocities for all registered avoidance agents.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>NavigationServer sends safe velocity vector with signals for each registered avoidance agents.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Agents receive the signal and move their parent e.g. with <literal>move_and_slide</literal> or <literal>linear_velocity</literal>.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>PhysicsServer synchronizes.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>physics frame ends.</paragraph>
                </list_item>
            </bullet_list>
            <paragraph>Therefore moving a physicsbody actor in the callback function with the safe velocity is perfectly thread- and physics-safe
                as all happens inside the same physics frame before the PhysicsServer commits to changes and does its own calculations.</paragraph>
            <substitution_definition names="weblate_widget"><reference refuri="https://hosted.weblate.org/engage/godot-engine/?utm_source=widget"><image alt="Translation status" candidates="{'?': 'https://hosted.weblate.org/widgets/godot-engine/-/godot-docs/287x66-white.png'}" height="66" uri="https://hosted.weblate.org/widgets/godot-engine/-/godot-docs/287x66-white.png" width="287"></image></reference></substitution_definition>
        </section>
    </section>
</document>
